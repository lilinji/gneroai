---
title: HPC常用命令（Linux）
sidebar_position: 3
description: >-
  太行I号智算平台Linux常用命令完整指南，涵盖文件操作、系统管理、Slurm作业调度等核心技能，助您高效使用超算资源。
keywords:
  - HPC命令
  - Linux基础
  - Slurm作业调度
  - 超算平台
  - 系统管理
  - 文件操作
---

# 🖥️ HPC常用命令（Linux）

本指南汇总了太行I号智算平台中Linux系统的常用命令，涵盖文件操作、系统管理、账户监控、Slurm作业调度等关键技能，帮助您快速掌握平台使用方法。

## 📋 目录

- [系统概述](#系统概述)
- [文件与目录操作](#文件与目录操作)
- [压缩与解压](#压缩与解压)
- [数据传输与文件管理](#数据传输与文件管理)
- [用户账户管理](#用户账户管理)
- [文件查看与文本操作](#文件查看与文本操作)
- [系统管理与进程监控](#系统管理与进程监控)
- [帮助与文档查看](#帮助与文档查看)
- [Slurm作业调度系统](#slurm作业调度系统)
- [实用技巧组合](#实用技巧组合)

## 🎯 系统概述

### Linux简介

**Linux** 是一种开源操作系统，以开放源代码为特点，允许用户自由查看、修改和分发，催生了众多发行版如Ubuntu和Debian。Linux适用于服务器、个人电脑及嵌入式设备，著称于其稳定性和可靠性，是HPC环境的标准选择。

### 平台环境
- **操作系统**：64位 RedHat 8.8
- **Shell环境**：Bash
- **用户目录**：`/hpcfs/fhome/username`
- **软件模块**：Environment Modules系统

## 📁 文件与目录操作

### 基础导航命令

- **`pwd`** - 显示当前所在目录的绝对路径

  *示例：*
  ```bash
  $ pwd
  /hpcfs/fhome/username
  ```

- **`cd`** - 切换目录

  *示例：*
  ```bash
  $ cd /etc
  $ cd ..         # 返回上一级目录
  $ cd ~          # 返回家目录
  $ cd -          # 返回上一个工作目录
  ```

- **`ls`** - 列出目录中的文件和子目录

  *常用选项：*
  - `-l`：长格式显示文件信息
  - `-a`：显示所有文件（包括隐藏文件）
  - `-h`：人性化显示文件大小
  
  *示例：*
  ```bash
  $ ls -la
  total 128
  drwx------  5 user123 hpc_users  4096 Jul 30 10:30 .
  drwxr-xr-x 15 root    root       4096 Jul 29 09:15 ..
  -rw-------  1 user123 hpc_users  2048 Jul 30 08:45 .bash_history
  drwxr-xr-x  3 user123 hpc_users  4096 Jul 29 14:20 project_data
  ```

### 创建与管理

- **`mkdir`** - 创建新目录

  *示例：*
  ```bash
  $ mkdir new_folder
  $ mkdir -p projects/analysis/data  # 创建多级目录
  ```

- **`rmdir`** - 删除空目录

  *示例：*
  ```bash
  $ rmdir empty_folder
  ```

- **`touch`** - 创建空文件或更新文件的访问、修改时间

  *示例：*
  ```bash
  $ touch new_file.txt
  $ touch simulation_results.log
  ```

### 复制、移动与删除

- **`cp`** - 拷贝文件或目录

  *常用选项：*
  - `-r`：递归复制目录
  - `-p`：保持文件属性
  
  *示例：*
  ```bash
  $ cp source.txt destination.txt
  $ cp -r source_dir/ destination_dir
  $ cp -p data.csv backup_data.csv  # 保持权限和时间戳
  ```

- **`mv`** - 移动或重命名文件、目录

  *示例：*
  ```bash
  $ mv oldname.txt newname.txt
  $ mv file.txt /path/to/directory/
  $ mv temp_results/ final_results/  # 重命名目录
  ```

- **`rm`** - 删除文件或目录

  *常用选项：*
  - `-r`：递归删除目录
  - `-f`：强制删除，不提示
  - `-i`：交互式删除（推荐）
  
  *示例：*
  ```bash
  $ rm file.txt
  $ rm -rf unwanted_directory/
  $ rm -i *.log  # 交互式删除所有日志文件
  ```

:::warning 删除操作警告
Linux系统没有回收站概念，删除操作不可逆。建议：
- 删除重要数据前先备份
- 使用 `-i` 选项进行交互确认
- 避免使用 `rm -rf /*` 等危险命令
:::

## 🗜️ 压缩与解压

### tar归档工具

- **`tar`** - 打包和压缩文件的标准工具

  *常用选项：*
  - `-c`：创建归档
  - `-x`：解压归档
  - `-v`：显示详细过程
  - `-f`：指定文件名
  - `-z`：通过gzip压缩/解压
  
  *示例：*
  ```bash
  # 创建压缩归档
  $ tar -czvf simulation_data.tar.gz simulation_folder/
  
  # 解压归档
  $ tar -xzvf simulation_data.tar.gz
  
  # 查看归档内容（不解压）
  $ tar -tzvf simulation_data.tar.gz
  ```

### gzip压缩工具

- **`gzip` / `gunzip`** - 压缩或解压单个文件

  *示例：*
  ```bash
  $ gzip large_dataset.txt        # 压缩为large_dataset.txt.gz
  $ gunzip large_dataset.txt.gz   # 解压恢复原文件
  $ gzip -k data.txt              # 保留原文件进行压缩
  ```

### zip压缩工具

- **`zip` / `unzip`** - 处理zip格式的压缩文件

  *示例：*
  ```bash
  $ zip -r project_backup.zip project_directory/
  $ unzip project_backup.zip
  $ unzip -l archive.zip          # 列出压缩包内容
  ```

## 🚀 数据传输与文件管理

### 安全文件传输

- **`scp`** - 通过SSH协议在本地和远程主机之间安全复制文件

  *示例：*
  ```bash
  # 从本地复制到远程服务器
  $ scp local_file.txt username@login.tibhpc.net:/hpcfs/fhome/username/
  
  # 从远程服务器复制到本地
  $ scp username@login.tibhpc.net:/path/to/remote_file.txt ./
  
  # 复制整个目录
  $ scp -r project_dir/ username@login.tibhpc.net:/hpcfs/fhome/username/
  ```

- **`rsync`** - 高效同步和传输文件或目录，支持断点续传

  *示例：*
  ```bash
  # 同步目录到远程服务器
  $ rsync -avz --progress local_directory/ username@login.tibhpc.net:/path/to/destination/
  
  # 仅同步更新的文件
  $ rsync -avz --update source/ destination/
  ```

- **`sftp`** - 交互式文件传输工具，基于SSH

  *示例：*
  ```bash
  $ sftp username@login.tibhpc.net
  sftp> ls                        # 查看远程目录
  sftp> get remote_file.txt       # 下载文件
  sftp> put local_file.txt        # 上传文件
  sftp> mget *.log               # 批量下载
  sftp> exit
  ```

## 👤 用户账户管理

### 基础用户信息

- **`whoami`** - 查看当前登录用户名

  *示例：*
  ```bash
  $ whoami
  user123
  ```

- **`id`** - 查看用户UID、GID及所属组

  *示例：*
  ```bash
  $ id user123
  uid=1001(user123) gid=1000(hpc_users) groups=1000(hpc_users),1001(project_alpha)
  ```

- **`groups`** - 查看用户所属组

  *示例：*
  ```bash
  $ groups user123
  user123 : hpc_users project_alpha gpu_users
  ```

### 存储配额管理

- **`quota`** - 查看用户存储配额及使用情况

  *常用选项：*
  - `-s`：以人类可读格式显示（GB/MB）
  - `-u username`：查看指定用户（需管理员权限）
  
  *示例：*
  ```bash
  $ quota -s
  Disk quotas for user user123 (uid 1001):
  Filesystem   space   quota   limit   grace   files   quota   limit   grace
  /hpcfs       15.2G   50.0G   55.0G           1234    10000   15000
  ```

### 作业队列监控

- **`squeue`** - 查看作业队列状态

  *常用选项：*
  - `-u username`：查看指定用户的作业
  - `-j jobid`：查看特定作业
  - `--start`：显示预计启动时间
  
  *示例：*
  ```bash
  $ squeue -u user123 --format="%.18i %.9P %.8j %.8u %.2t %.10M %.6D %R"
  JOBID     PARTITION   NAME     USER    STATE      TIME      NODES NODELIST
  1234567   cpu         sim1     user123 RUNNING    2:30:00   4     node[07-10]
  ```

- **`sacct`** - 查看历史作业记录

  *示例：*
  ```bash
  $ sacct -u user123 --starttime=2023-07-24 --format=JobID,JobName,Elapsed,State
  JobID           JobName     Elapsed      State
  1234567         sim1        00:30:00     COMPLETED
  1234568         analysis    01:15:30     COMPLETED
  ```

### 系统资源限制

- **`ulimit -a`** - 查看当前会话资源限制

  *示例：*
  ```bash
  $ ulimit -a
  core file size          (blocks, -c) 0
  data seg size           (kbytes, -d) unlimited
  scheduling priority             (-e) 0
  file size               (blocks, -f) unlimited
  pending signals                 (-i) 256085
  max memory size         (kbytes, -m) unlimited
  ```

### 实时监控工具

- **`htop`** - 实时查看进程资源占用

  *示例：*
  ```bash
  $ htop -u user123  # 过滤显示特定用户进程
  ```

- **`nvidia-smi`**（GPU集群）- 查看GPU使用情况

  *示例：*
  ```bash
  $ nvidia-smi --query-compute-apps=pid,used_gpu_memory --format=csv
  pid, used_gpu_memory [MiB]
  12345, 3245 MiB
  ```

- **`watch`** - 实时监控命令输出

  *示例：*
  ```bash
  # 每5秒刷新作业状态
  $ watch -n 5 "squeue -u $USER -o '%.8i %.15j %.2t %.10M %.6D %R'"
  ```

## 📄 文件查看与文本操作

### 文件内容查看

- **`cat`** - 查看文件内容，或将多个文件内容合并后输出

  *示例：*
  ```bash
  $ cat file.txt
  $ cat file1.txt file2.txt > merged.txt  # 合并文件
  ```

- **`less`** - 分页查看文件内容，支持向前/向后翻页

  *示例：*
  ```bash
  $ less large_file.txt
  # 按空格翻页，q退出，/搜索文本
  ```

- **`head` / `tail`** - 分别显示文件的前几行或后几行

  *示例：*
  ```bash
  $ head -n 10 data.csv    # 显示前10行
  $ tail -n 10 log.txt     # 显示后10行
  $ tail -f running.log    # 实时监控新增内容
  ```

### 文本搜索与处理

- **`grep`** - 在文件或命令输出中查找符合条件的字符串

  *常用选项：*
  - `-i`：不区分大小写
  - `-r`：递归搜索
  - `-n`：显示行号
  - `-v`：反向匹配
  
  *示例：*
  ```bash
  $ grep "error" logfile.txt
  $ grep -ri "configuration" /etc/
  $ grep -n "function" script.py
  ```

- **`find`** - 在目录中查找符合条件的文件

  *示例：*
  ```bash
  $ find /path/to/search -name "*.txt"
  $ find . -type f -size +100M        # 查找大于100MB的文件
  $ find . -name "*.log" -mtime -7    # 查找7天内修改的日志文件
  ```

## ⚙️ 系统管理与进程监控

### 进程管理

- **`ps`** - 查看当前运行的进程

  *常用选项：*
  - `aux`：显示所有进程的详细信息
  - `-u username`：显示指定用户的进程
  
  *示例：*
  ```bash
  $ ps aux
  $ ps -u user123 -o pid,ppid,cmd
  ```

- **`top` / `htop`** - 实时监控系统资源和进程信息

  *示例：*
  ```bash
  $ top
  $ htop                   # 更友好的界面
  ```

- **`kill`** - 终止指定进程

  *示例：*
  ```bash
  $ kill 1234              # 根据进程号终止进程
  $ kill -9 1234           # 强制终止进程
  $ killall python         # 终止所有python进程
  ```

### 系统资源监控

- **`free`** - 查看内存使用情况

  *示例：*
  ```bash
  $ free -h
                total        used        free      shared  buff/cache   available
  Mem:           62Gi       15Gi       32Gi       1.2Gi        14Gi        45Gi
  Swap:         8.0Gi          0B       8.0Gi
  ```

- **`df`** - 显示文件系统磁盘空间使用情况

  *示例：*
  ```bash
  $ df -h
  Filesystem      Size  Used Avail Use% Mounted on
  /dev/sda1        50G   35G   13G  74% /
  /hpcfs          10T   8.2T  1.8T  82% /hpcfs
  ```

- **`du`** - 显示目录或文件的磁盘占用情况

  *示例：*
  ```bash
  $ du -sh project_directory
  2.5G    project_directory
  
  $ du -h --max-depth=1 /hpcfs/fhome/username
  ```

### 系统信息

- **`uptime`** - 显示系统运行时间和负载情况

  *示例：*
  ```bash
  $ uptime
   14:25:38 up 42 days,  3:15,  5 users,  load average: 0.85, 1.23, 1.45
  ```

- **`uname`** - 显示系统和内核信息

  *常用选项：*
  - `-a`：显示全部信息
  - `-r`：显示内核版本
  
  *示例：*
  ```bash
  $ uname -a
  Linux login01 4.18.0-348.el8.x86_64 #1 SMP RedHat 8.8 x86_64 x86_64 x86_64 GNU/Linux
  ```

## 📚 帮助与文档查看

### 命令帮助系统

- **`man`** - 查看命令的使用手册

  *示例：*
  ```bash
  $ man ls
  $ man -k "copy"          # 搜索相关命令
  ```

- **`info`** - 显示更详细的帮助信息，有时比man更详细

  *示例：*
  ```bash
  $ info coreutils
  $ info tar
  ```

- **`whatis`** - 显示命令的一行简介

  *示例：*
  ```bash
  $ whatis ls
  ls (1) - list directory contents
  ```

- **`whereis`** - 查找命令的二进制文件、源代码及man手册的位置

  *示例：*
  ```bash
  $ whereis python
  python: /usr/bin/python3.8 /usr/lib/python3.8 /usr/share/man/man1/python3.8.1.gz
  ```

### 内置帮助

大多数命令都支持 `--help` 选项：

```bash
$ cp --help
$ tar --help
$ squeue --help
```

## 🎛️ Slurm作业调度系统

### 集群状态查看

- **`sinfo`** - 查看集群队列（分区）状态

  *常用选项：*
  - `-p <队列名>`：指定查看特定队列
  - `-o "%P %a %D %T %E"`：自定义输出格式
  - `-N`：显示节点级详细信息
  
  *示例：*
  ```bash
  # 查看GPU队列实时状态
  $ sinfo -p gpu -o "%P %a %D %T %20E"
  PARTITION AVAIL  NODES  STATE REASON
  gpu*         up      8   idle none
  gpu*         up      4  alloc none
  ```

### 资源分配与作业提交

- **`salloc`** - 分配资源并保持会话，用于后续交互式命令

  *常用选项：*
  - `-p <队列名>`：指定队列
  - `-N <节点数>`：分配节点数量
  - `--gres=gpu:<类型>:<数量>`：申请GPU
  - `--time=<时限>`：设置资源占用时间
  - `--exclusive`：独占节点资源
  
  *示例：*
  ```bash
  # 分配2节点、4块A100 GPU，限时2小时
  $ salloc -p gpu -N 2 --gres=gpu:a100:4 --time=2:00:00
  
  # 分配后使用srun执行任务
  $ srun -n 16 ./parallel_job
  ```

- **`srun`** - 动态分配资源并执行并行任务（交互式或批处理）

  *常用选项：*
  - `-p <队列名>`：指定目标队列
  - `-N <节点数>`：分配计算节点数量
  - `-n <任务数>`：指定并行任务总数（MPI进程数）
  - `--gres=gpu:<类型>:<数量>`：申请GPU资源
  - `--time=<时限>`：设置最大运行时间
  - `--pty`：启动交互式会话
  
  *示例：*
  ```bash
  # 在GPU队列启动交互式任务（分配1节点、2块A100 GPU）
  $ srun -p gpu -N 1 --gres=gpu:a100:2 --time=1:00:00 --pty /bin/bash
  
  # 直接运行MPI任务（4节点，每节点8进程）
  $ srun -p cpu -N 4 -n 32 mpirun ./simulation
  ```

- **`sbatch`** - 提交作业到指定队列

  *常用选项：*
  - `--partition=<队列名>`：指定目标队列
  - `--qos=<QOS策略>`：设置服务质量等级
  - `--time=<DD-HH:MM:SS>`：设置最大运行时间
  - `--nodelist=<节点列表>`：指定运行节点
  
  *示例：*
  ```bash
  # 提交到bigmem队列并限制运行时间
  $ sbatch --partition=bigmem --time=2-12:00:00 myjob.sh
  
  # 示例作业脚本
  $ cat << 'EOF' > gpu_job.sh
  #!/bin/bash
  #SBATCH --partition=gpu
  #SBATCH --gres=gpu:2
  #SBATCH --ntasks=1
  #SBATCH --cpus-per-task=8
  #SBATCH --time=1:00:00
  #SBATCH --job-name=gpu_training
  #SBATCH --output=job_%j.log
  
  # 加载必要模块
  module load python/3.9 cuda/11.8
  
  # 运行Python脚本
  python train_model.py
  EOF
  
  $ sbatch gpu_job.sh
  Submitted batch job 1234567
  ```

### 作业管理与控制

- **`scancel`** - 取消已提交作业

  *常用选项：*
  - `-p <队列名>`：取消指定队列的全部作业
  - `-u <用户名>`：操作用户级作业
  - `--state=<状态>`：按作业状态筛选（如PENDING/RUNNING）
  
  *示例：*
  ```bash
  # 取消特定作业
  $ scancel 1234567
  
  # 取消所有在debug队列中的待处理作业
  $ scancel -p debug --state=PENDING
  
  # 取消用户的所有作业
  $ scancel -u user123
  ```

### 作业状态监控

- **`squeue`** - 实时查看作业队列状态（已在用户账户管理章节介绍）

- **`sacct`** - 查看历史作业记录（已在用户账户管理章节介绍）

- **`scontrol show job`** - 查看作业详细信息

  *示例：*
  ```bash
  $ scontrol show job 1234567
  JobId=1234567 JobName=gpu_training
     UserId=user123(1001) GroupId=hpc_users(1000) MCS_label=N/A
     Priority=4294901757 Nice=0 Account=default QOS=normal
     JobState=RUNNING Reason=None Dependency=(null)
     Requeue=1 Restarts=0 BatchFlag=1 Reboot=0 ExitCode=0:0
  ```

## 🔧 实用技巧组合

### 管道与重定向

管道和重定向是Linux命令行的强大功能，可以组合多个命令完成复杂任务：

```bash
# 管道操作 - 将一个命令的输出作为另一个命令的输入
$ ps aux | grep python | wc -l              # 统计Python进程数量
$ cat data.txt | sort | uniq -c | head -10   # 统计并显示前10个最频繁的条目
$ squeue | grep RUNNING | awk '{print $1}'   # 获取所有运行中的作业ID

# 输出重定向
$ command > output.txt        # 将输出重定向到文件（覆盖）
$ command >> output.txt       # 追加输出到文件
$ command 2> error.log        # 将错误重定向到文件
$ command > output.txt 2>&1   # 同时重定向标准输出和错误输出
$ command &> all_output.txt   # 简化写法，同时重定向所有输出
```

### 后台任务管理

```bash
# 后台运行任务
$ long_running_script.sh &                    # 在后台运行脚本
$ nohup python train_model.py &               # 忽略挂起信号，防止SSH断开后任务终止
$ nohup python train.py > training.log 2>&1 & # 后台运行并重定向输出

# 作业控制
$ jobs                        # 查看当前shell的后台作业
$ fg %1                       # 将后台作业1调到前台
$ bg %1                       # 将暂停的作业1推到后台运行
$ disown -h %1                # 将作业从作业列表中移除，防止shell退出时终止
```

### 高效的文件查找

```bash
# 按名称查找文件
$ find /hpcfs/fhome/username -name "*.py" -type f

# 查找大文件（超过1GB）
$ find . -type f -size +1G -exec ls -lh {} \;

# 查找最近7天修改的文件
$ find . -type f -mtime -7 -ls

# 查找并删除临时文件
$ find /tmp -name "*.tmp" -type f -mtime +7 -delete

# 按权限查找文件
$ find . -type f -perm 755
```

### 文本处理技巧

```bash
# 统计和分析
$ wc -l *.txt                    # 统计所有txt文件的行数
$ grep -c "ERROR" *.log          # 统计日志文件中错误的数量
$ cut -d',' -f1,3 data.csv       # 提取CSV文件的第1和第3列

# 排序和去重
$ sort data.txt | uniq -c         # 排序并统计重复行
$ sort -k2,2nr results.txt        # 按第2列数字降序排序

# 字符串替换
$ sed 's/old_string/new_string/g' file.txt > new_file.txt
$ sed -i 's/path\/to\/old/path\/to\/new/g' config.txt     # 直接修改文件
```

### 系统监控组合

```bash
# 实时监控系统资源
$ watch -n 2 'free -h; echo; df -h'           # 每2秒刷新内存和磁盘使用
$ watch -n 5 'squeue -u $USER'                # 实时监控作业状态

# 进程监控
$ ps aux --sort=-%cpu | head -10              # 显示CPU使用率最高的10个进程
$ ps aux --sort=-%mem | head -10              # 显示内存使用率最高的10个进程

# 网络连接监控
$ netstat -tuln | grep :22                    # 查看SSH端口连接
$ ss -tuln                                     # 现代版的netstat
```

### 批量操作脚本

```bash
# 批量处理文件
$ for file in *.log; do
    echo "Processing $file"
    gzip "$file"                               # 压缩所有日志文件
done

# 批量创建作业脚本
$ for i in {1..10}; do
    cat > job_$i.sh << EOF
#!/bin/bash
#SBATCH --job-name=task_$i
#SBATCH --time=1:00:00
echo "Running task $i"
EOF
done

# 批量提交作业
$ for script in job_*.sh; do
    sbatch "$script"
done
```

### 环境模块系统

```bash
# 模块管理
$ module avail                    # 查看所有可用模块
$ module list                     # 查看已加载模块
$ module load python/3.9         # 加载Python 3.9模块
$ module load gcc/11.2.0 openmpi/4.1.0  # 同时加载多个模块
$ module unload python            # 卸载Python模块
$ module purge                    # 卸载所有模块

# 模块信息查看
$ module show python/3.9          # 显示模块详细信息
$ module spider python            # 搜索包含python的模块
$ module help python/3.9          # 显示模块帮助信息
```

### 一键式操作组合

```bash
# 清理临时文件并显示磁盘使用情况
$ rm -f /tmp/*.tmp; du -sh /tmp

# 检查系统状态的完整概览
$ echo "=== System Overview ==="; uname -a; echo; echo "=== CPU Info ==="; lscpu | grep "Model name\|CPU(s)"; echo; echo "=== Memory Usage ==="; free -h; echo; echo "=== Disk Usage ==="; df -h | grep -E "Filesystem|/hpcfs|/$"; echo; echo "=== Current Jobs ==="; squeue -u $USER

# 快速清理并准备新的计算任务
$ cd /hpcfs/fhome/$USER && mkdir -p new_project/{data,scripts,results} && cd new_project
```

### 故障诊断命令

```bash
# 系统负载检查
$ uptime; echo; top -bn1 | head -20          # 系统负载和进程快照
$ iostat -x 1 5                              # 磁盘I/O监控（5次，每秒一次）

# 作业问题诊断
$ scontrol show job JOBID                    # 查看作业详细信息
$ sacct -j JOBID --format=JobID,JobName,State,ExitCode,DerivedExitCode  # 作业退出状态

# 网络连接诊断
$ ping -c 4 login.tibhpc.net                 # 测试网络连接
$ traceroute login.tibhpc.net                # 追踪网络路径
```

## 📚 进阶学习建议

### 命令学习路径

掌握了这些命令后，建议按以下路径深化学习：

1. **基础巩固**
   - 熟练使用文件操作命令（`ls`, `cp`, `mv`, `rm`）
   - 掌握文本查看和搜索（`cat`, `less`, `grep`, `find`）
   - 理解管道和重定向概念

2. **HPC专业技能**
   - 深入学习Slurm作业调度系统
   - 掌握模块系统管理软件环境
   - 学会资源监控和故障排查

3. **自动化脚本**
   - 编写Bash脚本自动化重复任务
   - 学习使用`cron`定时执行任务
   - 掌握并行处理工具如GNU Parallel

### 实践练习建议

```bash
# 练习1：创建一个完整的项目目录结构
$ mkdir -p my_project/{data/{raw,processed},scripts,results,docs}
$ touch my_project/README.md

# 练习2：编写一个简单的作业脚本
$ cat > practice_job.sh << 'EOF'
#!/bin/bash
#SBATCH --job-name=practice
#SBATCH --time=0:10:00
#SBATCH --ntasks=1
echo "Job started at $(date)"
echo "Running on node: $(hostname)"
echo "Current directory: $(pwd)"
echo "Job completed at $(date)"
EOF

# 练习3：监控系统资源使用
$ watch -n 10 'echo "=== $(date) ==="; free -h; echo; df -h | grep hpcfs; echo; squeue -u $USER'
```

## 🎯 下一步学习

完成本章学习后，您可以继续：

- **[文件传输](./file-transfer)** - 学习高效的数据传输方法
- **[运行环境管理](../runtime-environment/overview)** - 配置软件运行环境
- **[Slurm作业系统](../slurm-job-system/overview)** - 深入学习作业调度
- **[软件应用](../software-applications/overview)** - 使用科学计算软件

## 📞 技术支持

### 学习资源

- **Linux基础教程**：[鸟哥的Linux私房菜](http://linux.vbird.org/)
- **Slurm官方文档**：[https://slurm.schedmd.com/](https://slurm.schedmd.com/)
- **BASH脚本编程**：[Advanced Bash-Scripting Guide](https://tldp.org/LDP/abs/html/)

### 联系我们

- **技术支持邮箱**：support@tib.cas.cn
- **命令行问题**：hpc-help@tib.cas.cn
- **工作时间**：周一至周五 9:00-18:00

## 🔖 常用命令速查卡

| 类别 | 常用命令 | 功能简述 |
|------|---------|----------|
| **导航** | `pwd`, `cd`, `ls` | 目录操作基础三件套 |
| **文件** | `cp`, `mv`, `rm`, `mkdir` | 文件和目录增删改查 |
| **查看** | `cat`, `less`, `head`, `tail` | 文件内容查看 |
| **搜索** | `grep`, `find`, `locate` | 文本和文件搜索 |
| **压缩** | `tar`, `gzip`, `zip` | 文件压缩解压 |
| **传输** | `scp`, `rsync`, `sftp` | 文件传输工具 |
| **监控** | `top`, `ps`, `free`, `df` | 系统资源监控 |
| **作业** | `sbatch`, `squeue`, `scancel` | Slurm作业管理 |
| **帮助** | `man`, `info`, `--help` | 获取命令帮助 |

---

<head>
  <title>HPC常用命令（Linux）- 太行I号用户指南</title>
  <meta
    name="description"
    content="太行I号智算平台Linux常用命令完整指南，涵盖文件操作、系统管理、Slurm作业调度等核心技能，助您高效使用超算资源。"
  />
  <meta name="keywords" content="HPC命令,Linux基础,Slurm作业调度,超算平台,系统管理,文件操作" />
</head>